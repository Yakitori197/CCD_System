# ============================================
# CCD 視覺檢測與自動剔除系統 - 依賴清單 (v1.2)
# Python 3.10 - 3.12
# ============================================

# 電腦視覺
opencv-python>=4.10,<5

# 數值計算
numpy>=1.26,<3

# YOLOv8/v11 深度學習框架
ultralytics>=8.3,<9

# GUI 圖像顯示
pillow>=10.3,<12

# 序列通訊（剔除裝置控制）
pyserial>=3.5,<4

# --------------------------------------------
# GPU 加速（可選，需要 NVIDIA GPU + CUDA）：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# --------------------------------------------

# --------------------------------------------
# 開發依賴（跑測試時安裝）：
#   pip install pytest
# --------------------------------------------
